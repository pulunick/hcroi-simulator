/**
 * 산식·등급·가정 화면은 빌드할 때 HTML 로 구워 둔다(프리렌더) — 검색 로봇이 자바스크립트를 돌리지
 * 않아도 본문을 읽을 수 있어야 하기 때문이다(네이버는 특히 그렇다). 루트 `+layout.ts` 의
 * `ssr = false` 를 이 화면에서만 덮는다.
 * 구워진 HTML 에는 금액 표시 단위·인원 산정 기준이 **기본값**으로 들어간다. 화면이 열리면
 * 저장된 설정으로 바로 갱신되므로 사용자가 보는 값은 언제나 자기 설정이다.
 */
export const ssr = true;
export const prerender = true;
