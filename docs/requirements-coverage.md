# 요구사항 커버리지 점검 (2026-09-01)

**대조 기준**: 기획서 원문은 이 레포에 없어, 기획서를 옮겨 적은 [spec.md](spec.md) 를 기준으로 대조했다.
spec.md 는 §2 수식을 "원문 그대로" 옮겼고 §5 는 기획서의 "응답규칙" 을 요약한 것이다.
**기획서 원문과 문장 단위로 대조하려면 원문 파일(md/docx/pdf)을 `docs/` 에 넣어 달라.**

## 1. 항목별 대조

| 기획서 항목 (spec 근거)                                              | 구현 위치                                         | 상태      | 비고                                                                 |
| -------------------------------------------------------------------- | ------------------------------------------------- | --------- | -------------------------------------------------------------------- |
| 인적자본 투입 전 이익 = 영업이익 + 총 인건비 (§2-1)                  | `formulas.ts` `computeMetrics` → `profitBeforeHc` | ✅        | 테스트 `formulas.test.ts`                                            |
| HCROI = (영업이익 + 총 인건비) ÷ 총 인건비 (§2-2)                    | `formulas.ts`, 대시보드 히어로·표, 시뮬레이터     | ✅        | 표기는 배수 1차 + % 보조 (§2 표기 규칙)                              |
| HCVA = … ÷ 총 임직원 수 (§2-3)                                       | `formulas.ts`, 대시보드 타일·표, 시뮬레이터 비교  | ✅        |                                                                      |
| 총 인건비 = 6항목 합 (§2-4)                                          | `sumHcCost`, 데이터 관리 세부 입력                | ✅        | 세부 없이 총액만 입력도 허용                                         |
| 정상성 진단 등급 (§3)                                                | `HCROI_THRESHOLDS`, `gradeOf`, `GradeBadge`       | ✅        | 1.3–1.5 "양호" 는 기획서 미정의 구간을 우리가 정의 (결정 2026-09-01) |
| 시뮬레이터 입력: 인원 변동율/인원수, 임금 인상률, 생산성 변화율 (§4) | `scenario.ts`, `/simulator` 슬라이더              | ✅        | 변동비 비율은 고급 옵션으로 추가                                     |
| 시뮬레이터 산출: 총 인건비'·매출'·영업이익'·HCROI'·HCVA' (§4)        | `runScenario`                                     | ✅        | `scenario.test.ts`                                                   |
| 손익분기 생산성 · 최대 임금 인상률 역산 (§4 부가)                    | `scenario.ts`, 시뮬레이터 "HCROI 유지 조건"       | ✅        | 닫힌 식, 테스트로 검증                                               |
| 악화 시 원인 분석 + 구조적 개선책 (§5)                               | `insights.ts` `scenarioInsights`                  | ✅        | ①~④ 개선책, 숫자·단위 명시                                           |
| 개선 시 지속가능성 점검 (§5)                                         | 동일                                              | ✅        | 고정비 가정 과대 추정 경고 추가 (2026-09-01)                         |
| 영업손실 전환 경고 (§5)                                              | 동일                                              | ✅        |                                                                      |
| 연도 추이 인사이트 (§5)                                              | `trendInsights`, 대시보드                         | ✅        | 연속 하락·전년 대비·인건비 vs 부가가치 증가율                        |
| 데이터 누락 시 기본값 안내 (§6)                                      | `defaults.ts`, 연도 추가 시 채움, 가이드 화면     | ✅        | "자사 실적으로 교체" 안내 명시                                       |
| 화면: 대시보드 / 시뮬레이터 / 데이터 / 가이드 (§7)                   | `src/routes/*`                                    | ✅        | 4개 화면 육안·상호작용 점검 완료                                     |
| 차트: 이중축 금지, 지표별 패널, 범례+직접 라벨+표 (§7)               | `charts/*`                                        | ✅        | 인라인 SVG                                                           |
| 저장: localStorage (§8)                                              | `workspace.svelte.ts`                             | ✅        | 키 `hcroi:workspace:v1`, JSON 내보내기/가져오기                      |
| 파일 교환: 엑셀 내보내기/템플릿/가져오기 (§8, 2026-09-01 추가 요구)  | `src/lib/hcroi/excel/*`, `/data`                  | ✅        | `excel.test.ts` 14건 + 브라우저 E2E 점검                             |
| 다음 단계 DB 스키마 준비 (§8)                                        | `supabase/migrations/…hcroi_schema.sql`           | ✅ 준비만 | 어느 프로젝트에도 미적용 (의도)                                      |

## 2. 기획서 문구를 해석해서 정한 것 (원문 확인 시 재검토 대상)

1. **"HCROI(%)"** → 배수 × 100 으로 해석, 화면은 배수 1차 · % 보조. 기획서가 %를 1차 표기로 요구했다면 `format.ts` 표기만 바꾸면 됨(계산 불변).
2. **1.3~1.5 구간** → ~~"양호" 로 정의~~ **해결(2026-09-03)**: 확장 지침(Martin 2.0)에 따라 보통에 통합, 3등급 체계로 변경.
3. **"응답규칙"(인사이트)** → 규칙 기반으로 구현. LLM 서술은 Phase 2 (그때 규칙 산출치를 프롬프트 근거로 전달).
4. **비인건비 처리** → 기획서에 명시가 없어 "기본 고정비 + 변동비 비율 옵션" 으로 정의.

## 3. 기획서 범위 중 프로토타입에서 뺀 것 (§9, Phase 2)

로그인/권한 · 다중 조직 · LLM 대화형 에이전트 · PDF 리포트 · 부서별 분해 · 다년 예측. (엑셀 업로드는 2026-09-01 구현으로 범위 안에 들어옴)
→ 기획서 원문에 이 중 **프로토타입 필수**로 적힌 항목이 있는지 원문으로 확인 필요.

## 4. 결론

spec.md 에 옮겨진 요구사항은 **전부 구현·테스트됨**(37 tests). 미확인 리스크는 "옮겨 적는 과정에서 빠진 항목" 뿐이며, 이는 원문 파일이 있어야 닫을 수 있다.
